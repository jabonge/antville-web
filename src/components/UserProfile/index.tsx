import React from 'react'
import { BarWrapper, PostWrapper, Wrapper } from '../../mds/styled/wrapper'
import SideBar from '../SideBar'

export default function UserProfile() {
  return (
    <Wrapper>
      <BarWrapper>
        <SideBar />
        <PostWrapper>dd</PostWrapper>
      </BarWrapper>
    </Wrapper>
  )
}
